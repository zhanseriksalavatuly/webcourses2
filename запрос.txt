/* ---------- 2. database.sql ---------- */
CREATE DATABASE web_courses;
USE web_courses;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50),
    password VARCHAR(255)
);

CREATE TABLE courses (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(100),
    video VARCHAR(255),
    image VARCHAR(255)
);